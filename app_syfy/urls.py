# coding=utf-8
from __future__ import unicode_literals
from django.conf.urls import include, url

urlpatterns = [
    # ---- Evento ----#
    # url(r'^evento/cadastrar/$', EventoCreateView.as_view(), name='evento-add'),
    # url(r'^evento/(?P<pk>[0-9]+)/visualizar/$', EventoDetailView.as_view(), name='evento-detail'),
    # url(r'^evento/(?P<pk>[0-9]+)/editar/$', EventoUpdateView.as_view(), name='evento-update'),
    # url(r'^evento/(?P<pk>[0-9]+)/excluir/$', EventoDeleteView.as_view(), name='evento-delete'),
    # url(r'^evento/listar/$', EventoListView.as_view(), name='evento-list'),
    # url(r'^evento/listar/$', TemplateView.as_view(template_name='../templates/appevento/evento_list.html'),
    #     name='evento-list'),
]